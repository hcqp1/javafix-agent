## 做了什么

<!-- 一两句话说明改了什么、为什么这么改 -->

## 自检清单

- [ ] 本地 `mvn test` 全绿
- [ ] 新增或改变的行为有测试覆盖；修 bug 的 PR 要能证明「修复前失败、修复后通过」
- [ ] **README 是否还准确**：只要改了输入输出契约、工具行为、默认值或整体流程（阶段/架构），
      必须同步更新 README。它过期的代价是别人（包括面试官）看到的第一印象就是错的
- [ ] 没有把调试残留带进来（临时日志、注释掉的代码、本地绝对路径、临时关闭的检查）
